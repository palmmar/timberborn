services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: timberborn
      POSTGRES_USER: timberborn
      POSTGRES_PASSWORD: timberborn
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
